<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.xaple.notekeeper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".OnBoarding"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" android:screenOrientation="portrait" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessageListActivity" android:screenOrientation="portrait"/>
        <activity android:name=".DownloadActivity" android:screenOrientation="portrait"/>
        <activity android:name=".UploadNoteActivity" android:screenOrientation="portrait"/>
        <activity android:name=".barCodeVerifierActivity" android:screenOrientation="portrait" />
    </application>

</manifest>